class student{ //Classes can have multiple functions

name(){
    var firstName="Laraib";
    var lastName="Fakhr";

    console.log(`Hi user:${firstName} ${lastName}` );
}

}
let s=new student() //We have to define the function using a var so that we can use
s.name()

